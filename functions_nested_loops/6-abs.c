#include "main.h"

/**
 * _abs - Write a function that computes the absolute value of an integer.
 * @n: int to check
 * Return: n (Succes)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
