#include "main.h"

/*
 * _islower - Write a function that checks for lowercase character.
 * Returns 1 if c is lowercase
 * Returns 0 otherwise
 *
 * Return: 0 (Succes)
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
