#include <string.h>

/**
 * _strlen - Write a function that returns the length of a string.
 * @s: char to check
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int x;

	x = strlen(s);
	return (x);
}
